{
  "name": "crossdock-test",
  "version": "1.0.0",
  "description": "Crossdock Test",
  "main": "index.js",
  "scripts": {
    "test": "cd auto_test && mocha reset_all.js --timeout 20000 && mocha order_management.js --timeout 20000 && mocha primary-shipment-plan.js --timeout 20000 && mocha secondary-shipment-plan.js --timeout 20000 && mocha assign-loading-bay.js --timeout 20000 && mocha associate-rollcage.js --timeout 20000 && mocha inbound.js --timeout 20000 && mocha putaway.js --timeout 20000 && mocha sort.js --timeout 20000 && mocha loading.js --timeout 20000 && mocha outbound.js --timeout 20000"
    },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brenthmiras/crossdock-test.git"
  },
  "keywords": [
    "express",
    "mysql",
    "boilerplate"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/brenthmiras/crossdock-test/issues"
  },
  "homepage": "https://github.com/brenthmiras/crossdock-test#readme",
  "dependencies": {
    "chai-http": "^4.0.0",
    "fs": "0.0.1-security"
  },
  "devDependencies": {
    "async": "^2.6.1",
    "chai": "^3.5.0",
    "csv-parse": "^2.5.0",
    "csv-stringify": "^3.1.1",
    "json2csv": "^4.2.1",
    "moment": "^2.22.2",
    "readline-sync": "^1.4.9",
    "supertest": "^2.0.1"
  }
}
